apiVersion: v1
kind: PersistentVolume
metadata:
  name: nfs001
spec:
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Recycle
  storageClassName: gold-nfs
  mountOptions:
    - hard
  nfs:
    path: /share
    server: 192.168.99.1
